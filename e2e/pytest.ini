[pytest]
# A stack real (docker compose) precisa já estar de pé, respondendo
# em localhost:8090, antes de rodar esta suíte - ver README.md desta
# pasta ou .github/workflows/e2e.yml para como isso é orquestrado.
addopts = --base-url=http://localhost:8090
